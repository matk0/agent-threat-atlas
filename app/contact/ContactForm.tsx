// Deprecated: the /contact route now redirects to /about.
// This file is kept to avoid a stale import error if anything still references it.
export default function ContactForm() {
  return null;
}
